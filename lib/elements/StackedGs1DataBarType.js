/**Represents Stacked GS1 DataBar types. */
export const stackedGs1DataBarType = Object.freeze({

    /** Stacked type. */
    "stacked": 0,

    /** Stacked Omnidirectional type. */
    "stackedOmnidirectional": 1,

    /** Expanded stacked type. */
    "expandedStacked": 2
});