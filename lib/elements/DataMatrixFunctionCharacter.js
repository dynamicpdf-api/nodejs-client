/**Specifies the Function Character for a Data matrix barcode. */
export const dataMatrixFunctionCharacter = Object.freeze({

    /** Macro 05 Character. */
    "macro05": 0,

    /** Macro 06 Character. */
    "macro06": 1,

    /** ReaderProgramming Character. */
    "readerProgramming": 2,

    /** FNC1 Character. */
    "fnc1": 3,

    /** None. */
    "none": 4
});