/** Specifies EncodingType for Data matrix bar codes.*/
export const dataMatrixEncodingType = Object.freeze({

    /** Calculates Encoding based on Value. */
    "auto": 0,

    /** Calculates ASCII Encoding based on Value. */
    "autoAscii": 1,

    /** ASCII Encoding. */
    "ascii": 2,

    /** Extended ASCII Encoding. */
    "extendedAscii": 3,

    /** Double digit Encoding. */
    "doubleDigit": 4,

    /** C40 Encoding. */
    "c40": 5,

    /** Text Encoding. */
    "text": 6,

    /** ANSI X12 Encoding. */
    "ansiX12": 7,

    /**EDIFACT Encoding. */
    "edifact": 8,

    /** Base256 Encoding. */
    "base256": 9,
    
});