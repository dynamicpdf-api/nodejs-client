/** Specifies zoom type. */
export const pageZoom = Object.freeze({

    /** Keep unchanged. */
    "retain": 0,

    /** Fit page in window. */
    "fitPage": 1,

    /** Fit width of page in window. */
    "fitWidth": 2,

    /** Fit height of page in window. */
    "fitHeight": 3,

    /** Fit all elements of page in window. */
    "fiBox": 4
});