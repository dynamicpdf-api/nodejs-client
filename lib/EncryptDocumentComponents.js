/** Specifies the document components to be encrypted. */
export const EncryptDocumentComponents = Object.freeze({

    /** Encrypts all document contents. */
    "All": 0,

    /** Encrypts all document contents except metadata. */
    "AllExceptMetadata": 1
});