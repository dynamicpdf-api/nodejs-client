/** Specifies the style of the outline. */
export const outlineStyle = Object.freeze({

    /**Bold. */
    "bold": 0,

    /**BoldItalic. */
    "boldItalic": 1,

    /** Italic. */
    "italic": 2,

    /** Regular. */
    "regular": 3
});